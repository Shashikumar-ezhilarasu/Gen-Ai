# How it Works — Slide content outline

1. Neural networks recap (one diagram)
2. LLMs: autoregressive vs encoder-decoder — diagram + training objective
3. Tokens & tokenization: BPE/WordPiece example + visual
4. Training process: dataset, loss, compute, scaling laws
5. Inference: sampling (greedy, top-k, top-p), temperature
6. Diffusion models pipeline (image/audio)
7. Takeaway slide
